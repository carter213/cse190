<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >    


    <Button
        android:id="@+id/main_search"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
		android:text="@string/search"
    	android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:onClick="openSearchDialog" />
    
    <Button
        android:id="@+id/nearby"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
		android:text="@string/search_nearby"
    	android:layout_centerHorizontal="true"
        android:layout_below="@+id/main_search"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:onClick="doSearchNearby" />
   
     <Button
        android:id="@+id/login"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
		android:text="@string/login"
    	android:layout_centerHorizontal="true"
        android:layout_below="@+id/nearby"
        android:layout_marginTop="50dp"
        android:gravity="center"
        android:onClick="doLogin" />

     <Button
         android:id="@+id/main_signUpBtn"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_below="@+id/login"
         android:layout_centerHorizontal="true"
         android:layout_marginTop="83dp"
         android:gravity="center"
         android:onClick="doSignup"
         android:text="Sign Up" />

     <Button
         android:id="@+id/main_profileBtn"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_alignParentBottom="true"
         android:layout_alignRight="@+id/main_signUp_btn"
         android:layout_marginBottom="20dp"
         android:onClick="doProfile"
         android:text="user profile" />

     <Button
         android:id="@+id/main_signOutBtn"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_alignBaseline="@+id/main_profileBtn"
         android:layout_alignBottom="@+id/main_profileBtn"
         android:layout_toRightOf="@+id/main_signUpBtn"
         android:onClick="doSignOut"
         android:text="Sign Out" />
    
</RelativeLayout>